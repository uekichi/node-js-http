doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが強いですか？
    form(method="post" action=path)
      span 名前:
      input(type="text" name="name")
      input(type="radio" name="stronger" value=firstItem)
      span #{firstItem}
      input(type="radio" name="stronger" value=secondItem)
      span #{secondItem}
      button(type="submit") 投稿